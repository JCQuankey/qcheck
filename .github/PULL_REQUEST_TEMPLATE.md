<!-- Describe what this PR changes and why. -->

## Safety checklist

- [ ] No secrets or API keys
- [ ] No private docs
- [ ] No paid API calls
- [ ] No real benchmark rows unless explicitly approved
- [ ] No AI-authorship metadata or content

<!--
Auto-merge (optional): add the `automerge` label for a low-risk change (docs,
copy, website, generated site, tests). Changing the site generator also needs
`safe-public`. Auto-merge only runs on same-repo PRs, never touches protected
paths, and merges only after required status checks pass.
-->

<!-- denylist smoke test: this PR touches .github and must NOT auto-merge -->
